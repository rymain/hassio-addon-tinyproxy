#!/command/with-contenv bashio
# shellcheck shell=bash
bashio::log.info "Tinyproxy exited with code ${1} (signal ${2})"

if [[ "${1}" -ne 0 ]] && [[ "${1}" -ne 256 ]]; then
    bashio::log.warning "Halting add-on."
    exec /run/s6/basedir/bin/halt
fi
